{
  "name": "hyperveil-keeper",
  "private": true,
  "type": "module",
  "description": "HyperVeil keeper: crosses Veil orders internally, routes the rest to Hyperliquid through the omnibus, reports HyperCore fills, relays CCTP.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1136.0",
    "ethers": "^6.13.0",
    "starknet": "^10.0.2",
    "veil-sdk": "file:../../sdk"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
